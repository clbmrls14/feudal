

const functions = {
    save: [
       window.save = function() {
            alert("you saved the game");
        }
        
         
    
    ],
     load: [
        window.load = function(){
            alert("you saved the game");
        }

     ],
};

export default functions;